{
  "name": "playwright-gad-tests",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
  "test": "playwright test",
  "test:seq": "playwright test --workers=1",
  "allure:generate": "npx allure generate allure-results --clean -o allure-report",
  "allure:open": "npx allure open allure-report",
  "allure:report": "npx allure generate allure-results --clean -o allure-report && npx allure open allure-report",
  "test:compare": "playwright test --project=chromium --workers=1",
"test:crossbrowser": "playwright test --project=chromium --project=firefox --project=webkit"
},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/adm-zip": "^0.5.7",
    "@types/node": "^24.10.1",
    "adm-zip": "^0.5.16",
    "allure-commandline": "^2.37.0",
    "allure-playwright": "^3.6.0"
  }
}
